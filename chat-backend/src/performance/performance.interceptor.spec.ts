import { PerformanceInterceptor } from './performance.interceptor';

describe('PerformanceInterceptor', () => {
  it('should be defined', () => {
    expect(new PerformanceInterceptor()).toBeDefined();
  });
});
